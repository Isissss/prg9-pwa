import { ProjectT } from '@/app/page';
import React from 'react';    
 
export default function ProjectCard({ project, links  }: ProjectT) {
   const { title, description, header_image, screenshots } = project;

    return (
       <div>
         
       </div>
    )
  }